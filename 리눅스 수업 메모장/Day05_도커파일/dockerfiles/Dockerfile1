# 베이스 이미지 지정 (ubuntu)
FROM ubuntu:22.04

# Dockerfile1 파일로 이미지를 빌드(docker build)한 뒤,
# 해당 이미지로 컨테이너를 실행(docker run)할 때 수행할 명령어 지정
CMD ["echo", "Hello World"]

### 실행방법
### 1. 초기 작업1) 서버에 dockerfiles 디렉터리 생성 (한 번만 수행)
###    goodee@server1:~$ mkdir ~/dockerfiles
### 2. 초기 작업2) dockerfiles 디렉터리로 이동 (한 번만 수행)
###    goodee@server1:~$ cd dockerfiles/
### 3. 공통 작업) 모든 도커 파일은 dockerfiles 디렉터리에 복사해 둬야 합니다. (도커 파일마다 매번 수행)
###    MobaXterm의 SFTP 화면으로 도커 파일을 드래그하면 됩니다.
### 4. 이미지 빌드 (이미지 만들기)
###    -t myubuntu:1.0 : 이미지 이름과 태그
###    -f Dockerfile1  : 도커 파일 이름 지정 (도커 파일 이름이 Dockerfile인 경우 생략)
###    .               : 현재 디렉터리(.)에 Dockerfile1 파일이 존재
###    goodee@server1:~/dockerfiles$ docker build -t myubuntu:1.0 -f Dockerfile1 .
### 5. 컨테이너 실행
###    -d  : 백그라운드 실행 시 결과를 확인할 수 없으므로 사용하지 않음
###    -rm : 컨테이너가 중지되면 자동 삭제
###    1) CMD 그대로 사용
###    goodee@server1:~/dockerfiles$ docker run --rm myubuntu:1.0
###    Hello World
###    2) CMD 변경 (이미지명 다음에 전달하는 인자는 CMD로 전달)
###    goodee@server1:~/dockerfiles$ docker run --rm myubuntu:1.0 echo "Hi World"
###    Hi World